# ============================================================
# .gitignore — Pattaya.Gym repo
# ============================================================

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Local + secrets
.env
.env.local
private/outreach/*.csv
*.log
*.bak
*.tmp
node_modules/
tmp/

# Build / CI artifacts
.lighthouseci/
lh-*.json
lighthouse-*.json
push-output.txt

# Backup archives — never commit these to repo
.backups/
*.zip
*.tar.gz
*.tar
BACKUP_MANIFEST_*.md
pattayagym_*.zip
pattayagym_*.tar.gz

# Google Drive sync artifacts
.tmp.driveupload/

# Misc binary garbage Cowork mount sometimes leaves
zirZ3Bwy
zii7NKzl
$null

# Cloudflare Wrangler local state (Round 17 — Codex F07.3)
.wrangler/
wrangler-account.json

# Empty extensionless garbage files that periodically appear (Round 17 — Codex F07.3)
# PUSH_ROUND17.cmd has a sweep step that deletes these; this just keeps any
# newly-created ones from being committed by accident.
30
Venue
af-academy-pattaya
git
/kids-youth
kids-youth)
main

# Operator docs - local only, never published.
# Regenerate with: deploy.ps1 -SyncRules
CLAUDE.md
AGENTS.md
RULES.md
CHANGELOG-STRUCTURAL.md
# loop concurrency run lock (operational state)
.internal-docs/RUN-LOCK.json
